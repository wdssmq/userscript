{
  "scripts": {
    "build:script": "pnpm -r --filter '!lib-empty' run build",
    "git:script": "git diff-index --quiet HEAD || (git commit -m \"build. dist/\" -a --no-verify)",
    "build": "npm run build:script && pnpm lint:fix",
    "plop": "plop --plopfile=template/plopfile.js",
    "lint": "eslint",
    "lint:fix": "eslint --fix"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "cross-env": "catalog:",
    "eslint": "catalog:",
    "plop": "catalog:",
    "tsx": "catalog:"
  }
}
