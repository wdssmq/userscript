{
  "type": "module",
  "scripts": {
    "build": "cross-env NODE_ENV=prod rollup -c",
    "dev": "cross-env NODE_ENV=dev rollup -c -w",
    "clear": "rm -rf dev"
  },
  "dependencies": {
    "@rollup/plugin-replace": "^4.0.0",
    "cross-env": "^7.0.3",
    "rollup": "^2.78.1",
    "rollup-plugin-monkey": "^1.0.3"
  }
}
